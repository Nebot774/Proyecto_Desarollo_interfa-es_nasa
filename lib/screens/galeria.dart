import 'package:flutter/material.dart';

class galeria extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Galería'),
      ),
      body: Center(
        child: Text('Contenido de la Galería'),
      ),
    );
  }
}
