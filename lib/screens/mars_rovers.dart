import 'package:flutter/material.dart';

class mars_rovers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('mars_rovers'),
      ),
      body: Center(
        child: Text('mars_rovers'),
      ),
    );
  }
}