import 'package:flutter/material.dart';

class asteroides_cercanos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('asteroides_cercanos'),
      ),
      body: Center(
        child: Text('Contenido de asteroides_cercanos'),
      ),
    );
  }
}
