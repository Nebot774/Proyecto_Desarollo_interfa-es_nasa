import 'package:flutter/material.dart';

class imagen_del_dia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('imagen_del_dia'),
      ),
      body: Center(
        child: Text('imagen_del_dia'),
      ),
    );
  }
}